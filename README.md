# 3NF-VS-Star-schema

## convert a normalized 3NF schema into a star schema to optimize analytical queries.

## I compared query performance by calculation of the time taken by each query and found that the star schema significantly reduced execution time due to fewer joins and denormalization, making it more suitable for OLAP workloads.

